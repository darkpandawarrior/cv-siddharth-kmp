.gradle/
build/
**/build/
!gradle/wrapper/gradle-wrapper.jar
.kotlin/
local.properties
.idea/
*.iml
.DS_Store

# Secrets — never commit (see scripts/setup-secrets.sh)
keystore.properties
*.jks
*.keystore
secrets.properties

# Kotlin/Wasm dependency lockfile dir — regenerated by the browser tasks
kotlin-js-store/

# Code knowledge graph (graphify). Derived, rebuildable, and large; it also mirrors every
# symbol name in the repo, so it is never committed. Rebuild: node ~/Tools/DevTools/AgentHarness/scripts/graph.mjs build
graphify-out/
